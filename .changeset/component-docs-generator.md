---
---

Extend `turbo gen component` to scaffold the component docs page (`<name>.mdx`) and add its `meta.json` nav entry. Tooling only; no published package changes, so this changeset carries no version bump.
