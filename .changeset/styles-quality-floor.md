---
"@friday-sandbox/styles": minor
---

Quality floor for the token system: perceptual `color-mix(in oklab)` across the color ladder, a distinct radius ramp (`field` 0.375rem / `box` 0.75rem / new `feedback`), five distinct button label sizes (adds `label-xs` and `label-xl` so no two adjacent sizes are pixel-identical), a populated base layer (border-color inheritance, `html` background/foreground/font, `::selection`), and named scalar/ratio tokens (`--space`, `--disabled-opacity`, `--ring-width`, `--ring-offset-width`).
