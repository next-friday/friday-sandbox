---
"@friday-sandbox/styles": minor
---

Add a superset of semantic color roles (`muted`, `card`, `popover`, `input`, and a `destructive` alias for `danger`) and the dark-mode primitive values the system previously lacked, so themes render in both light and dark and the vocabulary interoperates with the wider ecosystem.
