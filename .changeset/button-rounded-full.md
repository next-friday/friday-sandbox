---
"@friday-sandbox/react": minor
"@friday-sandbox/styles": minor
---

Add an `isRoundedFull` prop to `Button` for a fully rounded button. It pairs with `isIconOnly` for a circular icon button. Exposed in CSS as the addressable `.fri-button-rounded-full` class.
