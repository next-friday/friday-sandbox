---
"@friday-sandbox/styles": minor
---

Generate `design.md` (the human- and machine-readable theme contract) and `tokens/knobs.json` (the theme-builder manifest) from the spec, schema and formula table. Adds the `./knobs` export and publishes `design.md`, so the upcoming theme builder and AI agents can read the contract and the editable knob set from one source of truth.
