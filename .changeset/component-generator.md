---
---

Add a `turbo gen component` generator that scaffolds a base component across the React files, the three export barrels, the styles css and its import, and a changeset. Tooling only; no published package changes, so this changeset carries no version bump.
