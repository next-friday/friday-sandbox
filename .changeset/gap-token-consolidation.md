---
"@friday-sandbox/styles": patch
---

Consolidate the gap utilities onto the spacing scale and remove the redundant `--fri-gap-*` token aliases. Gap classes now resolve through `--fri-spacing-*`, so there is no visual change.
