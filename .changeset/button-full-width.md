---
"@friday-sandbox/react": minor
"@friday-sandbox/styles": minor
---

Add an `isFullWidth` prop to `Button` for a button that spans the full width of its container. Exposed in CSS as the addressable `.fri-button-full-width` class.
