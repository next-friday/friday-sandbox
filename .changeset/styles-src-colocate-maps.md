---
"@friday-sandbox/styles": major
"@friday-sandbox/react": major
---

Restructure the styles package under `src/` and co-locate each component's Tailwind Variants map with its React component. Theme tokens are hand-authored with per-mode mix-ratio seeds, and the neutral `fill` role is renamed to `muted`.
