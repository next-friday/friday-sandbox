---
---

Add the `new-component` project skill that drives the full component build — generate, then fill the variants, styles, stories, and docs against the repo conventions. Tooling only; no published package changes.
