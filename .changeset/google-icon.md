---
"@friday-sandbox/react": minor
---

Add a `GoogleIcon` component, the multi-color Google logo, for sign-in buttons and similar. It accepts the standard SVG props, so size it with `width` / `height` or a `className`.
