---
"@friday-sandbox/styles": minor
---

Register `--surface` and `--surface-foreground` in `@theme inline` so the `bg-surface` and `text-surface-foreground` Tailwind utilities are available alongside the other color tokens.
