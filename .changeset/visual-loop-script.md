---
"@friday-sandbox/react": minor
---

Added a `visual` package script: `pnpm --filter @friday-sandbox/react run visual <component>` boots (or reuses) Storybook and screenshots every story of the component to a temp directory for a pre-push visual check.
