---
"@friday-sandbox/styles": minor
---

Introduce a single derivation authority: `src/theme/color.css` and `src/theme/theme.css` are now generated by `scripts/codegen.js` from `tokens/default.spec.json` and the `scripts/formulas.js` formula table (loop-generated, removing the hand-duplicated per-role ladder). Dark mode now uses `light-dark()` at `:root`, so the interaction ladder's surface mixes reflow per `color-scheme` — fixing dark surfaces that previously mixed against the light background. Adds `./schema` and `./tokens` export entry points (the theme-spec JSON Schema and the default spec) for downstream tooling.
