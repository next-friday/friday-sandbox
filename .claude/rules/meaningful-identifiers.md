---
paths:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.mts"
  - "**/*.cts"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.mjs"
  - "**/*.cjs"
---

# Meaningful Identifiers

**Rule:** Every identifier names what it represents — no one- or two-letter shorthands, no `tmp`/`val`/`obj`/`fn`/`cb`/`el`/`tw`. Intent lives in names, commits, and `docs/`, not in prose comments.

Canonical definition, read it before editing matching files: [`docs/tooling/rules/meaningful-identifiers.md`](../../docs/tooling/rules/meaningful-identifiers.md). This file is a Claude Code pointer that auto-loads the rule by path; the full text lives in `docs/`.
