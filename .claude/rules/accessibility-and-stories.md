---
paths:
  - "packages/react/src/**/*.stories.{ts,tsx}"
  - "packages/react/src/**/*.test.{ts,tsx}"
---

# Accessibility and Stories

**Rule:** Every interactive component is keyboard-operable and screen-reader-correct, and every Storybook story proves it while reading as consumer documentation.

Canonical definition, read it before editing matching files: [`docs/react/accessibility-and-stories.md`](../../docs/react/accessibility-and-stories.md). This file is a Claude Code pointer that auto-loads the rule by path; the full text lives in `docs/`.
