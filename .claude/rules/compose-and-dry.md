---
paths:
  - "packages/react/src/**/*.{ts,tsx}"
---

# Compose, Don't Reinvent

**Rule:** Compose `react-aria-components` for interaction and the `layouts` primitives for structure; extract shared component logic into a hook under `src/` instead of repeating it.

Canonical definition, read it before editing matching files: [`docs/react/compose-and-dry.md`](../../docs/react/compose-and-dry.md). This file is a Claude Code pointer that auto-loads the rule by path; the full text lives in `docs/`.
