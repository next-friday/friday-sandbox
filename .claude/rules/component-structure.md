---
paths:
  - "packages/react/src/**/*.{ts,tsx}"
---

# Component Structure

**Rule:** Every `@friday-sandbox/react` component follows one symmetric folder skeleton under `src/components/<tier>/<name>/` — same files, naming, and exports as the `button` reference.

Canonical definition, read it before editing matching files: [`docs/react/rules/component-structure.md`](../../docs/react/rules/component-structure.md). This file is a Claude Code pointer that auto-loads the rule by path; the full text lives in `docs/`.
