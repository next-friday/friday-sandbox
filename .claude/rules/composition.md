---
paths:
  - "packages/react/src/**/*.{ts,tsx}"
---

# Composition and Reuse

**Rule:** Compose `react-aria-components` for interaction and the `layouts` primitives for structure; extract shared component logic into a hook under `src/` instead of repeating it.

Canonical definition, read it before editing matching files: [`docs/react/rules/composition.md`](../../docs/react/rules/composition.md). This file is a Claude Code pointer that auto-loads the rule by path; the full text lives in `docs/`.
