---
title: Spinner
description: An indeterminate loading indicator.
---

import { Flex, Spinner } from "@friday-sandbox/react";

## Purpose

The `Spinner` signals that work is in progress. It renders a `<span>`, on its own
or inside another component, such as `Button` while `isPending`.

## When to use

Use `Spinner` for an indeterminate wait, where the duration is unknown.

## When not to use

Do not use `Spinner` for determinate progress. Show a progress indicator with a
known percentage instead.

## Example

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="end" gap="md" className="px-4 py-6">
      <Spinner />
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    <Spinner />
    ```
  </Tab>
</Tabs>

## Colors

The `color` prop matches the semantic colors used across the system.

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="end" gap="md" className="px-4 py-6">
      <Spinner color="primary" />
      <Spinner color="secondary" />
      <Spinner color="accent" />
      <Spinner color="info" />
      <Spinner color="success" />
      <Spinner color="warning" />
      <Spinner color="danger" />
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    <Spinner color="primary" />
    <Spinner color="secondary" />
    <Spinner color="accent" />
    <Spinner color="info" />
    <Spinner color="success" />
    <Spinner color="warning" />
    <Spinner color="danger" />
    ```
  </Tab>
</Tabs>

## Sizes

Five sizes step from `xs` to `xl`.

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="end" gap="md" className="px-4 py-6">
      <Spinner size="xs" />
      <Spinner size="sm" />
      <Spinner size="md" />
      <Spinner size="lg" />
      <Spinner size="xl" />
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    <Spinner size="xs" />
    <Spinner size="sm" />
    <Spinner size="md" />
    <Spinner size="lg" />
    <Spinner size="xl" />
    ```
  </Tab>
</Tabs>

## Props

`Spinner` also accepts the standard `<span>` props, except `color`, which is the
variant prop above, and a `className` string.

| Prop        | Type                                                                                   | Default     |
| ----------- | -------------------------------------------------------------------------------------- | ----------- |
| `color`     | `"primary" \| "secondary" \| "accent" \| "info" \| "success" \| "warning" \| "danger"` | `"primary"` |
| `size`      | `"xs" \| "sm" \| "md" \| "lg" \| "xl"`                                                 | `"md"`      |
| `className` | `string`                                                                               | —           |

## Accessibility

- The spinner is decorative; convey loading state in the surrounding context,
  such as a `Button` with `isPending` or text like "Loading…".
- If a standalone spinner is the only indication of a busy region, give that
  region `role="status"` and an accessible label so screen readers announce it.
