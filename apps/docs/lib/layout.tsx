import type { BaseLayoutProps } from "fumadocs-ui/layouts/shared";

/** Shared layout options reused by the home and docs layouts. */
export const baseOptions: BaseLayoutProps = {
  nav: {
    title: "Friday",
  },
  githubUrl: "https://github.com/next-friday/friday-sandbox",
};
