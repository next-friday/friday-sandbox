---
title: {{ pascalCase name }}
description: {{ pascalCase name }} component.
---

import { {{ sortedNamedImports "Flex" (pascalCase name) }} } from "@friday-sandbox/react";

<SourceLinks
  source="https://github.com/next-friday/friday-sandbox/blob/main/packages/react/src/components/bases/{{ kebabCase name }}/{{ kebabCase name }}.tsx"
  style="https://github.com/next-friday/friday-sandbox/blob/main/packages/styles/src/components/{{ kebabCase name }}.css"
  storybook="STORYBOOK_URL"
  headless="HEADLESS_DOCS_URL"
/>

{/* Replace STORYBOOK_URL with this component's published Storybook page, and HEADLESS_DOCS_URL with the wrapped library's component docs; drop the `headless` prop when the wrapped part has no standalone page. */}

## Import

```tsx
import { {{ pascalCase name }} } from "@friday-sandbox/react";
```

## Usage

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="center" gap="md" p="md">
      <{{ pascalCase name }}>{{ pascalCase name }}</{{ pascalCase name }}>
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    <{{ pascalCase name }}>{{ pascalCase name }}</{{ pascalCase name }}>
    ```
  </Tab>
</Tabs>

## Purpose

Describe what `{{ pascalCase name }}` is for in a line or two.

## When to use

{/* List the cases where `{{ pascalCase name }}` is the right choice. */}

## When not to use

{/* List the cases where another component or element fits better, and name it. */}

## Sizes

Five sizes step from `xs` to `xl`.

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="center" gap="md" p="md">
      <{{ pascalCase name }} size="xs">Extra small</{{ pascalCase name }}>
      <{{ pascalCase name }} size="sm">Small</{{ pascalCase name }}>
      <{{ pascalCase name }} size="md">Medium</{{ pascalCase name }}>
      <{{ pascalCase name }} size="lg">Large</{{ pascalCase name }}>
      <{{ pascalCase name }} size="xl">Extra large</{{ pascalCase name }}>
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    <{{ pascalCase name }} size="xs">Extra small</{{ pascalCase name }}>
    <{{ pascalCase name }} size="sm">Small</{{ pascalCase name }}>
    <{{ pascalCase name }} size="md">Medium</{{ pascalCase name }}>
    <{{ pascalCase name }} size="lg">Large</{{ pascalCase name }}>
    <{{ pascalCase name }} size="xl">Extra large</{{ pascalCase name }}>
    ```
  </Tab>
</Tabs>

{/* Add a Tabs block per remaining feature (colors, variants, states) as its own `##` section between Sizes and Props as you fill the ladder; mirror the showcase stories. */}

## Props

`{{ pascalCase name }}` also accepts every prop of the react-aria primitive it wraps, plus a `className` string.

| Prop        | Default | Type                                                        |
| ----------- | ------- | ---------------------------------------------------------- |
| `size`      | `"md"`  | `"xs" \| "sm" \| "md" \| "lg" \| "xl"`<br />Sets the size of the {{ kebabCase name }}. |
| `className` | —       | `string`<br />Additional CSS classes to apply.             |

## Styling

`{{ pascalCase name }}` is styled through CSS classes, so the look applies to any element that carries them.

### Modifier classes

| Class | Type | Description |
| --- | --- | --- |
| `fri-{{ kebabCase name }}` | Base | Base styles: md size default. |
| `fri-{{ kebabCase name }}-xs` | Size | Extra small size. |
| `fri-{{ kebabCase name }}-sm` | Size | Small size. |
| `fri-{{ kebabCase name }}-md` | Size | Medium size (default). |
| `fri-{{ kebabCase name }}-lg` | Size | Large size. |
| `fri-{{ kebabCase name }}-xl` | Size | Extra large size. |

{/* Add a row per remaining fri-{{ kebabCase name }}-<value> class (colors, variants) as you fill the ladder; enumerate each named step as its own row, and compact a dense integer sequence into a single range row (see button.mdx / grid.mdx). */}

### State selectors

State styling responds to these React Aria data attributes. Add a row for each
`[data-*]` selector as you fill in the states:

| Selector | Applies when |
| --- | --- |
| `[data-focus-visible]` | The {{ kebabCase name }} is focused by keyboard. |
| `[data-disabled]` | The {{ kebabCase name }} is disabled. |

## Accessibility

Document keyboard interaction, ARIA roles, and any state caveats.
