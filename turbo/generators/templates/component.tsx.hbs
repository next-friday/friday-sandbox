import type { ComponentPropsWithRef } from "react";

import { {{ camelCase name }}Variants } from "@friday-sandbox/styles/components/{{ kebabCase name }}";
import type { {{ pascalCase name }}Variants } from "@friday-sandbox/styles/components/{{ kebabCase name }}";

export interface {{ pascalCase name }}Props
  extends ComponentPropsWithRef<"div">,
    {{ pascalCase name }}Variants {
  className?: string;
}

export const {{ pascalCase name }} = (props: Readonly<{{ pascalCase name }}Props>) => {
  const { children, className, ...rest } = props;

  const resolvedClassName = {{ camelCase name }}Variants({ class: className });

  return (
    <div data-slot="{{ kebabCase name }}" className={resolvedClassName} {...rest}>
      {children}
    </div>
  );
};
