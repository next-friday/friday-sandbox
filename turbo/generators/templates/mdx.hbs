---
title: {{ pascalCase name }}
description: {{ pascalCase name }} component.
---

import { Flex, {{ pascalCase name }} } from "@friday-sandbox/react";

## Purpose

Describe what `{{ pascalCase name }}` is for in a line or two.

[Source](https://github.com/next-friday/friday-sandbox/blob/main/packages/react/src/components/bases/{{ kebabCase name }}/{{ kebabCase name }}.tsx) · [Style](https://github.com/next-friday/friday-sandbox/blob/main/packages/styles/src/components/bases/{{ kebabCase name }}.css) · [Storybook](STORYBOOK_URL) · [Headless](HEADLESS_DOCS_URL)

{/* Replace STORYBOOK_URL with the deployed Storybook story link, and HEADLESS_DOCS_URL with the wrapped library's component docs; drop the Headless link when the wrapped part has no standalone page (a plain HTML element). */}

## When to use

{/* List the cases where `{{ pascalCase name }}` is the right choice. */}

## When not to use

{/* List the cases where another component or element fits better, and name it. */}

## Example

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="center" gap="md" className="px-4 py-6">
      <{{ pascalCase name }} />
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    import { {{ pascalCase name }} } from "@friday-sandbox/react";
    <{{ pascalCase name }} />
    ```
  </Tab>
</Tabs>

{/* Add one Tabs block per feature you expose, each as its own `##` section between Example and Props; mirror the showcase stories in {{ kebabCase name }}.stories.tsx so docs and Storybook stay symmetric. */}

## Props

`{{ pascalCase name }}` also accepts the props of the element or primitive it wraps, plus a `className` string.

| Prop        | Type     | Default | Description                      |
| ----------- | -------- | ------- | -------------------------------- |
| `className` | `string` | —       | Additional CSS classes to apply. |

## Styling

`{{ pascalCase name }}` is styled through CSS classes ([source styles](https://github.com/next-friday/friday-sandbox/blob/main/packages/styles/src/components/bases/{{ kebabCase name }}.css)), so the look applies to any element that carries them.

### Base class

- `fri-{{ kebabCase name }}` — base `{{ kebabCase name }}` styles.

### State modifier classes

{/* List each fri-{{ kebabCase name }}-<modifier> class you define (e.g. fri-{{ kebabCase name }}-disabled), with the state it applies. Remove this section if the component has no modifier classes. */}

## Accessibility

Document keyboard interaction, ARIA roles, and any state caveats.
