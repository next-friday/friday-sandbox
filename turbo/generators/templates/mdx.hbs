---
title: {{ pascalCase name }}
description: {{ pascalCase name }} component.
---

import { {{ sortedNamedImports "Flex" (pascalCase name) }} } from "@friday-sandbox/react";

<SourceLinks
  source="https://github.com/next-friday/friday-sandbox/blob/main/packages/react/src/components/bases/{{ kebabCase name }}/{{ kebabCase name }}.tsx"
  style="https://github.com/next-friday/friday-sandbox/blob/main/packages/styles/src/components/{{ kebabCase name }}.css"
  storybook="STORYBOOK_URL"
  headless="HEADLESS_DOCS_URL"
/>

{/* Replace STORYBOOK_URL with this component's published Storybook page, and HEADLESS_DOCS_URL with the wrapped library's component docs; drop the `headless` prop when the wrapped part has no standalone page (a plain HTML element). */}

## Import

```tsx
import { {{ pascalCase name }} } from "@friday-sandbox/react";
```

## Usage

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <Flex align="center" gap="md" p="md">
      <{{ pascalCase name }} />
    </Flex>
  </Tab>

  <Tab value="Code">
    ```tsx
    <{{ pascalCase name }} />
    ```
  </Tab>
</Tabs>

## Purpose

Describe what `{{ pascalCase name }}` is for in a line or two.

## When to use

{/* List the cases where `{{ pascalCase name }}` is the right choice. */}

## When not to use

{/* List the cases where another component or element fits better, and name it. */}

{/* Add a `## Variants` or `## Sizes` section here only when you add that axis, mirroring its showcase story in {{ kebabCase name }}.stories.tsx. Every use-case story added later gets its own same-named `##` section here (component-docs syncs them). */}

## Props
{{#if compound}}

### {{ pascalCase name }} Props

`{{ pascalCase name }}` accepts the props of the element or primitive it wraps, plus a `className` string.

| Prop        | Type     | Default | Description                      |
| ----------- | -------- | ------- | -------------------------------- |
| `className` | `string` | —       | Additional CSS classes to apply. |
{{#each subparts}}

### {{ pascalCase ../name }}.{{ this }} Props

`{{ pascalCase ../name }}.{{ this }}` accepts the props of the element or primitive it wraps, plus a `className` string.

| Prop        | Type     | Default | Description                      |
| ----------- | -------- | ------- | -------------------------------- |
| `className` | `string` | —       | Additional CSS classes to apply. |
{{/each}}
{{else}}

`{{ pascalCase name }}` also accepts the props of the element or primitive it wraps, plus a `className` string.

| Prop        | Type     | Default | Description                      |
| ----------- | -------- | ------- | -------------------------------- |
| `className` | `string` | —       | Additional CSS classes to apply. |
{{/if}}

## Styling

`{{ pascalCase name }}` is styled through CSS classes, so the look applies to any element that carries them.

### Modifier classes

| Class | Type | Description |
| --- | --- | --- |
| `fri-{{ kebabCase name }}` | Base | Base `{{ kebabCase name }}` styles. |

{/* Add one row per fri-{{ kebabCase name }}-<value> class you define; enumerate each named step (size, color, variant) as its own row, and compact a dense integer sequence into a single range row `| \`fri-{{ kebabCase name }}-<first>\` … \`-<last>\` | <Type> | <first> to <last>. |` (see button.mdx / grid.mdx). */}

{/* If the component carries React Aria data-state styling, add a `### State selectors` table below with `| Selector | Applies when |` columns, one `[data-*]` row each — mirror the aria template / button.mdx. */}

## Accessibility

Document keyboard interaction, ARIA roles, and any state caveats.
