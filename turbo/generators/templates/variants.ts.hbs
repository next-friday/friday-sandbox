import { cva } from "class-variance-authority";
import type { StrictVariantProps } from "../../utils/variant-props";

export const {{ camelCase name }}Variants = cva("fri-{{ kebabCase name }}", {
  variants: {},
  defaultVariants: {},
});

export type {{ pascalCase name }}Variants = StrictVariantProps<typeof {{ camelCase name }}Variants>;
