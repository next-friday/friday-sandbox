@layer components {
  /* {{ pascalCase name }} — visual rules for the fri-{{ kebabCase name }} class.
     Replace with the component's geometry, color, and variant rules. */
  .fri-{{ kebabCase name }} {
    @apply block;
  }
}
