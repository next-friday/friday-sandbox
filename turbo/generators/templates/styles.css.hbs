@layer components {
  .fri-{{ kebabCase name }} {
    {{!-- @apply utilities here — see button.css. A property Tailwind has no utility
       for becomes a named @utility in layers/utilities.css (see status-disabled), then @apply it. --}}
  }
}
