import { expect } from "storybook/test";
import type { Meta, StoryObj } from "@storybook/react-vite";

import { {{ pascalCase name }} } from ".";

const meta = {
  title: "Bases/{{ category }}/{{ pascalCase name }}",
  component: {{ pascalCase name }},
  tags: ["autodocs"],
  parameters: {
    docs: {
      description: {
        component: [
          "{{ pascalCase name }} component.",
          "",
          "## Import",
          "",
          "```tsx",
          'import { {{ pascalCase name }} } from "@friday-sandbox/react";',
          "```",
        ].join("\n"),
      },
    },
  },
  argTypes: {
    className: {
      description: "Additional CSS classes to apply to the {{ kebabCase name }}.",
      control: "text",
      table: { type: { summary: "string" } },
    },
  },
} satisfies Meta<typeof {{ pascalCase name }}>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  render: (storyArgs) => (
    <{{ pascalCase name }} {...storyArgs}>{{ pascalCase name }}</{{ pascalCase name }}>
  ),
  play: async ({ canvasElement }) => {
    const element = canvasElement.querySelector('[data-slot="{{ kebabCase name }}"]');

    await expect(element).toBeInTheDocument();
  },
};
{{!-- Showcase trio only (.claude/rules/stories-docs-sync.md): add a Variants or
   Sizes story only when you add that axis (a color or shape axis joins the
   Variants grid) — lint:symmetry checks axis → story → doc section. Every
   further story is a designer use case, authored after ship and mirrored by a
   same-named ## section (component-docs). --}}
