import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Tokens" />

# Tokens

Override any of these in your app's CSS to retheme.

## Intent colors

Eight brand colors. Same value across light and dark — components handle text contrast for you.

<div
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
    gap: "0.75rem",
    marginTop: "1rem",
  }}
>
  {[
    "primary",
    "secondary",
    "accent",
    "neutral",
    "info",
    "success",
    "warning",
    "danger",
  ].map((name) => (
    <div key={name}>
      <div
        style={{
          height: "3rem",
          borderRadius: "var(--radius-action)",
          background: `var(--${name})`,
        }}
      />
      <code style={{ fontSize: "0.75rem", marginTop: "0.25rem", display: "block" }}>
        --{name}
      </code>
    </div>
  ))}
</div>

## Surfaces

Light by default; flipped by `.dark` or `[data-theme="dark"]`.

| Token                | Role                                  |
| -------------------- | ------------------------------------- |
| `--background`       | Body surface                          |
| `--foreground`       | Body text on `--background`           |
| `--muted`            | Secondary surface (placeholder, etc.) |
| `--muted-foreground` | Text on `--muted`                     |

## Sizing scopes

| Scope    | Tokens                            | Component family                          |
| -------- | --------------------------------- | ----------------------------------------- |
| `action` | `--size-action`, `--radius-action` | button, icon-button, link-button         |
| `field`  | `--size-field`, `--radius-field`   | input, textarea, select, checkbox, radio |
| `box`    | `--size-box`, `--radius-box`       | card, modal, alert, popover              |

### Default sizing

At the default `--size-{scope}` of `0.25rem`, the five sizes resolve to:

| Size | Height            |
| ---- | ----------------- |
| `xs` | 1.5rem (24 px)    |
| `sm` | 2rem (32 px)      |
| `md` | 2.5rem (40 px)    |
| `lg` | 3rem (48 px)      |
| `xl` | 3.5rem (56 px)    |

Override `--size-action` (or `--size-field` / `--size-box`) and every size for that scope rescales together.

`--radius-{scope}` is the reference at `md`. Smaller sizes get proportionally smaller corners, larger sizes get larger ones, so roundness stays balanced from `xs` to `xl`.

## Miscellaneous

| Token      | Role                  |
| ---------- | --------------------- |
| `--border` | Default border width  |
