import { Meta, Title, Subtitle } from "@storybook/addon-docs/blocks";

<Meta title="Get started" />

<Title>@friday-sandbox/react</Title>

<Subtitle>
  Accessible React UI components themed with CSS variables.
</Subtitle>

Components ship with keyboard navigation, focus rings, hover, and press states built in. Colors, sizes, and corner radius are CSS variables: override one and every component updates.

This release ships Button, Flex, Grid, GridItem, and ScrollArea.

## Install

```sh
pnpm add @friday-sandbox/react @friday-sandbox/styles
```

Peer dependencies: `react@^19`, `react-dom@^19`, `tailwindcss@^4`.

## Usage

Import Tailwind and the stylesheet once at the application root. Tailwind must come first, or components render unstyled:

```css
@import "tailwindcss";
@import "@friday-sandbox/styles";
```

If your setup imports CSS through JavaScript instead, use the side-effect import after Tailwind is loaded:

```ts
import "@friday-sandbox/styles";
```

Then import components:

```tsx
import { Button } from "@friday-sandbox/react";

export function Example() {
  return <Button variant="primary">Click me</Button>;
}
```

## Theming

Light is the `:root` default. Switch to dark with the `.dark` class or the `[data-theme="dark"]` attribute on `<html>`:

```html
<html class="dark">
  …
</html>
<!-- or -->
<html data-theme="dark">
  …
</html>
```

The Storybook toolbar exposes the same switch under the **Theme** control. See the [Theming](?path=/docs/theming--docs) page for color, surface, and sizing overrides.

## Next

- [Tokens](?path=/docs/tokens--docs) for the full list of overridable CSS variables.
- [Button](?path=/docs/bases-actions-button--docs), [Flex](?path=/docs/bases-layout-flex--docs), [Grid](?path=/docs/bases-layout-grid--docs), and [ScrollArea](?path=/docs/bases-layout-scrollarea--docs) for the component APIs.
- [github.com/next-friday/friday-sandbox](https://github.com/next-friday/friday-sandbox) for the source.
